/* inline style file */
const styles = {
  root: {
    position: 'fixed',
    width: '100%',
    height: '100%',
    top: 0,
    left: 0,
    zIndex: 1000,
    background: 'rgba( 0, 0, 0, 0.2)'
  },

  viewerBar: {
    height: 64
  },

  view: {
    
  }
};

export default styles;
